Makefile
out
sim/results
sim/.*
.settings
# OMNeT++-built binaries at the project root (anchored so they do NOT swallow
# the loti-core sources under src/, e.g. src/core/loti_core.cpp, src/app/loti)
/loti
/loti_dbg
/loti_release
/loti.exe
/loti_dbg.exe
/loti*.dll
*.so
*_m.h
*_m.cc
# CMake build tree (loti-core / product)
/build/
# proof artifacts written by `loti prove … --out`
*.loti
