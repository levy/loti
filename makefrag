#
# Use the new message compiler introduced in OMNeT++ 5.3
#
MSGC:=$(MSGC) --msg6

#
# loti-core is C++20 (defaulted comparisons, etc.). The whole sim build includes
# core headers, so compile everything as C++20 (overrides OMNeT++'s default).
#
CXXFLAGS += -std=c++20
